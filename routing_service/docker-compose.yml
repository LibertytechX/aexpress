version: '3.8'

services:
  routing-service:
    build: .
    container_name: routing-service
    ports:
      - "8071:8081"
    env_file:
      - .env
    environment:
      - ADDR=:8081
    restart: unless-stopped
